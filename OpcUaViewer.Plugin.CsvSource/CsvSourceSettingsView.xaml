<UserControl x:Class="OpcUaViewer.Plugin.CsvSource.CsvSourceSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
            CornerRadius="4" Padding="16">
        <StackPanel>
            <Label Content="Production Log Folder"/>
            <TextBlock Text="Path to the folder where the machine writes daily *_ProductionLog.csv files."
                       Foreground="{StaticResource TextSubBrush}" FontSize="11" Margin="0,0,0,6"
                       TextWrapping="Wrap"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0"
                         Text="{Binding LogFolderPath, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center"/>
                <Button Grid.Column="2" Content="Browse…" Padding="16,6"
                        Click="BrowseButton_Click"/>
            </Grid>

            <!-- History import -->
            <Separator Margin="0,16,0,12"/>
            <Label Content="Historical Import"/>
            <TextBlock Text="On startup, import completed parts from past log files to pre-populate the Stats view. Files are only imported once."
                       Foreground="{StaticResource TextSubBrush}" FontSize="11" Margin="0,0,0,8"
                       TextWrapping="Wrap"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Days to look back:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox Text="{Binding HistoryDaysBack, UpdateSourceTrigger=PropertyChanged}"
                         Width="60" VerticalContentAlignment="Center"/>
                <TextBlock Text="(0 = disabled)" Foreground="{StaticResource TextSubBrush}"
                           VerticalAlignment="Center" Margin="8,0,0,0" FontSize="11"/>
            </StackPanel>

            <!-- Status -->
            <Grid Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Ellipse Grid.Column="0" Width="10" Height="10" VerticalAlignment="Center">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="{StaticResource TextSubBrush}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                    <Setter Property="Fill" Value="{StaticResource SuccessBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>

                <TextBlock Grid.Column="2" Text="{Binding StatusText}"
                           Foreground="{StaticResource TextSubBrush}"
                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
